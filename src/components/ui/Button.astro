---
const styleOptions = {
  icon: "icon",
  primary: "primary",
  secondary: "secondary",
} as const;

type Props = {
  tagName?: "a" | "button";
  class?: string;
  style: keyof typeof styleOptions;

  isDefault?: boolean;
  [key: string]: any;
};
const {
  class: classList,
  tagName = "button",
  style,
  isDefault = true,
  ...rest
} = Astro.props;

const Tag = tagName;
---

<Tag
  class:list={[
    "button",
    classList,
    styleOptions[style],
    { default: isDefault },
  ]}
  {...rest}
>
  <slot />
</Tag>

<style></style>
